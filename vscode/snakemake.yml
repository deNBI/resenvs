---
- name: Install Snakemake
  pip:
    name: "{{ item }}"
  loop:
    - snakemake
  remote_user: ubuntu
  become: false

- name: Install VSCode Snakemake Lanquage Support Plugin
  ansible.builtin.shell: code-server --install-extension snakemake.snakemake-lang --user-data-dir=/home/"{{ vscode_vars.DEFAULT_USER  }}"/.local/share/code-server
  args:
    executable: /bin/bash

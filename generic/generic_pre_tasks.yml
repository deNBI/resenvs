---
- name: Populate service facts
  ansible.builtin.service_facts:

- name: Wait until Apt_Mirror de.NBI Bielefeld Service is done
  ansible.builtin.systemd:
    name: de.NBI_Bielefeld_environment.service
  register: denbi_environment_service
  until: denbi_environment_service.status.ActiveState == "inactive"
  retries: 35
  delay: 10
  when: "'de.NBI_Bielefeld_environment.service' in ansible_facts.services"

- name: PRE_TASK Disable unattended upgrades
  become: true
  ansible.builtin.lineinfile:
    path: /etc/apt/apt.conf.d/10periodic
    regexp: '^APT::Periodic::Unattended-Upgrade'
    line: 'APT::Periodic::Unattended-Upgrade "0";'
    create: true

- name: PRE_TASK Stop apt background services
  become: true
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: stopped
    enabled: false
  loop:
    - unattended-upgrades.service
    - apt-daily.service
    - apt-daily.timer
    - apt-daily-upgrade.service
    - apt-daily-upgrade.timer
  failed_when: false

- name: PRE_TASK Wait for apt and dpkg processes
  become: true
  ansible.builtin.shell: |
    while pgrep -x apt >/dev/null \
       || pgrep -x apt-get >/dev/null \
       || pgrep -x dpkg >/dev/null \
       || pgrep -x unattended-upgr >/dev/null; do
      sleep 5
    done
  changed_when: false
  retries: 60
  delay: 5

- name: PRE_TASK Configure pending dpkg packages
  become: true
  ansible.builtin.command:
    cmd: dpkg --configure -a
  changed_when: false

- name: PRE_TASK Update apt cache
  become: true
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
    lock_timeout: 300

- name: PRE_TASK Upgrade packages
  become: true
  ansible.builtin.apt:
    upgrade: dist
    lock_timeout: 300

- name: PRE_TASK Remove unused packages
  become: true
  ansible.builtin.apt:
    autoremove: true
    lock_timeout: 300

- name: PRE_TASK Clean apt cache
  become: true
  ansible.builtin.apt:
    autoclean: true

- name: Install Python pip
  become: true
  ansible.builtin.apt:
    name: python3-pip
    state: present

- name: Install monitoring denbi telegraf
  become: true
  ansible.builtin.apt:
    name: denbi-monitoring-conf
    state: latest
  when:
    - location is defined
    - location == "bielefeld"

- name: Setup Docker
  ansible.builtin.import_tasks: ../generic/setup_docker.yml

- name: Install Metadata-Service
  ansible.builtin.import_tasks: ../generic/install_metadata_service.yml

---
- hosts: all
  become: true
  name: Cluster Master Setup
  vars_files:
    - vars/common_configuration.yml
  roles:
    - common
    - autoscaling
    - geerlingguy.docker
    - master
    - role: slurm
      tags:
        - slurm
        - scale-up
        - scale-down
        - autoscaling

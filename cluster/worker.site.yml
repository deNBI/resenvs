- hosts: localhost
  become: 'yes'
  vars_files:
  - vars/common_configuration.yml
  roles:
  - geerlingguy.docker
  - common
  - worker
  - role: slurm
    tags:
    - slurm
    - scale-up
    - scale-down
